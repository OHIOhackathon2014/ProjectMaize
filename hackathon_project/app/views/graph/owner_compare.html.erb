<h1><%= "Repositories of #{params[:owner]}" %></h1>
<%= javascript_include_tag "http://code.highcharts.com/highcharts.js", "chartkick" %>
<%= line_chart @repos.map {|r|
      {name: @names[@repos.index(r)], data: r.map{|c|
      c.date}.zip(r.map{|c| c.changes})}}
%>
